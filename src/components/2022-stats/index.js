import SectionHead from "../sectionHeads";

export default function V1PostEventStats() {
  return (
    <section className="flex flex-col my-8 mx-14 sm:mx-2 bg-primary_extra_light  rounded-2xl p-10 w-full">
      <SectionHead sectionName={"We hacked well in 2022, so as in 2024"} />

      <div className="flex  items-center justify-evenly m-auto gap-6 lg:flex-col-reverse mt-6">
        <div className="w-quarter_past mr-14 lg:m-auto"></div>
      </div>
    </section>
  );
}
